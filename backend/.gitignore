node_modules
# Keep environment variables out of version control
.env

# Build output — Render rebuilds on every deploy
/dist
*.tsbuildinfo

/generated/prisma

# Upload migration audit logs
scripts/migrate-uploads-*.jsonl

# WhatsApp Baileys session files (auth credentials)
whatsapp-session/
